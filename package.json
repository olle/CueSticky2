{
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "flyps": "^0.1.0",
    "gh-pages": "^2.0.1",
    "jest": "^24.8.0",
    "parcel": "^1.12.3"
  },
  "scripts": {
    "test": "jest",
    "serve": "parcel index.html",
    "build": "parcel build src/**/*.js",
    "deploy": "gh-pages -d dist"
  }
}
